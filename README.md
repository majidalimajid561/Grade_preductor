# Grade_preductire
The data use for it is toy data set that build by random function 
The grade depend on `study_hours`, `attendance`, `past_score` features

- **Algorithms Used:** `LinearRegression()`
